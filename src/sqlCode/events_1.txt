CREATE TABLE IF NOT EXISTS events_1 
(
id VARCHAR(255) NOT NULL,
name VARCHAR(255) NOT NULL,
created INT NOT NULL,
time INT NOT NULL, 
utc_offset INT NOT NULL, 
status VARCHAR(255) NOT NULL, 
visibility VARCHAR(255) NOT NULL, 
headcount INT NOT NULL, 
rsvp_limit INT NOT NULL, 
maybe_rsvp_count INT NOT NULL, 
yes_rsvp_count INT NOT NULL, 
waitlist_count INT NOT NULL, 
venue_id INT DEFAULT NULL, 
group_id INT NOT NULL
);

COPY events_1 FROM 'D:\Arquivos e Programas\Eclipse\workpace\RecEventos\data_csv\events_1.csv' DELIMITERS ',' CSV HEADER;